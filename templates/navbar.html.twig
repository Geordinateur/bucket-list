<nav>
    <ul>
        <li><a href="{{ path('home') }}">Accueil</a></li>
        <li><a href="{{ path('about') }}">About us</a></li>
        <li><a href="{{ path('wish_list') }}">Liste des idées</a></li>
        <li><a href="{{ path('category_list') }}">Liste des catégories</a></li>
        {% if is_granted("ROLE_USER") %}
            <li><a href="{{ path('app_logout') }}">Se deconnecter</a></li>
            <li>Hi {{ app.user.email }}</li>
        {% else %}
            <li><a href="{{ path('app_login') }}">Se connecter</a></li>
        {% endif %}
    </ul>
</nav>